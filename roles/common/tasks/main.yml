
- name: Install packages
  become: yes
  ansible.builtin.package:
    name: "{{ item }}"
    state: present
  with_items:
    # - apt-transport-https
    - ca-certificates
    - curl
    # - gnupg-agent
    # - software-properties-common
    # - python3-pip
    # - python3-setuptools
    # - aptitude
    # - golang
    - git
    - bash-completion
    # - nfs-common
    # - dphys-swapfile
    - lvm2
    - xfsprogs
    - btrfs-progs
    - rsync
    - lsb-release
